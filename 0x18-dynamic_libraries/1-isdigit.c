#include "main.h"

/**
 * _isdigit - Custom function that checks for digit 0 through 9
 * @c : The name of the variable
 * Return: 1 if c is a digit, 0 otherwise
 */

int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
